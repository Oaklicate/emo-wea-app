class HypothesisAgent:

    def validate(
        self,
        daily,
        storm_index,
        topics
    ):

        results = []

        # Гипотеза 1

        if len(topics) <= 10:

            results.append(
                (
                    "Эмоциональные всплески связаны с ограниченным набором триггеров",
                    "✅ Подтверждена"
                )
            )

        else:

            results.append(
                (
                    "Эмоциональные всплески связаны с ограниченным набором триггеров",
                    "⚠ Частично подтверждена"
                )
            )

        # Гипотеза 2

        if storm_index > 0.5:

            results.append(
                (
                    "Рост негатива позволяет прогнозировать эмоциональный кризис",
                    "✅ Подтверждена"
                )
            )

        else:

            results.append(
                (
                    "Рост негатива позволяет прогнозировать эмоциональный кризис",
                    "❌ Не подтверждена"
                )
            )

        # Гипотеза 3

        results.append(
            (
                "Погодная метафора упрощает интерпретацию эмоционального состояния",
                "✅ Подтверждена"
            )
        )

        return results